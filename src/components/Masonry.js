import React from "react";

export default function Masonry() {
  return <div>balls</div>;
}

//if middle column, only need border on top and bottom. Left and right columnes can then have borders on all sides
